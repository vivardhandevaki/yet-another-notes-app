# Implementation Tasks

- [ ] Add only `review.ci_mode: advisory` to the root enforcement configuration.
- [ ] Run the sealed `CiReviewPolicyTest` oracle and confirm it becomes green.
- [ ] Run Crucible verify without editing sealed artifacts, bound tests, or the concurrent `create-note` change.
