# Spec delta: <capability>

## ADDED Requirements

### Requirement: <name> [REQ-<slug>-<seq>]

The system SHALL <normative statement — use SHALL/MUST, not should/may>.

#### Scenario: <scenario name>

- **WHEN** <trigger>
- **THEN** <observable outcome>

<!-- Every requirement needs a bracketed [REQ-...] id and at least one Scenario
     (exactly four hashes). Use ## MODIFIED / REMOVED / RENAMED Requirements for
     changes to existing behavior. -->
