# Oracles

<!-- A refactor adds NO new oracles. Leave this file with only this title.
     Correctness is the regression suite: every past promise must still hold.
     Adding a `## ORC-...` oracle here fails the bundle closed (exit 3). -->
